#==============================================================================
# Lumina OS – Package List (chroot)
#==============================================================================
# Packages that will be installed into the live system.
#==============================================================================

# --- Base system & utilities ---
live-boot
live-config
live-config-systemd
sudo
curl
wget
git
rsync
ca-certificates
apt-transport-https

# --- Network & Mesh ---
# yggdrasil is installed via its own repository / first-boot logic

# --- AI Swarm related ---
python3
python3-pip
python3-venv
curl

# --- System tools ---
htop
tmux
vim
nano
less

# Note: Ollama itself is installed via the official install script
# (see scripts/setup-ollama.sh) because it is not in the main Debian repos.
